# PS便捷菜单 独立版 — 使用说明

## 快速开始
1. 把本目录整个拷到任意固定位置（如 `D:\Tools\PSMenu\`）。
2. 双击 `PSMenu.exe` 启动，托盘出现图标（深色方块+鼠标指针）。
3. 呼出菜单三选一：
   - 全局热键 **Ctrl + Alt + Space**（可在 设置 → 启动与触发 里改）
   - **Photoshop 前台按鼠标中键**（设置里可关）
   - 托盘左键单击

## 功能速查
| 想做什么 | 去哪 |
|---|---|
| 改菜单项/子菜单/快捷键 | 设置 → 图层菜单 |
| 换主题/颜色/光效 | 设置 → 外观主题 |
| 多套配置来回切 | 设置 → 配置管理 → 配置方案（托盘也有子菜单） |
| 改热键/中键/开机自启 | 设置 → 启动与触发 |
| 从脚本库加脚本 | 设置 → 图层菜单 → ＋ 从脚本库 |
| 导出/导入配置 | 设置 → 配置管理（JSON 复制到剪贴板） |

## 数据位置
- 配置：`%APPDATA%\PSMenu\ps_cm_config.json`（首次启动自动从 Quicker 目录迁移）
- 快照：`%APPDATA%\PSMenu\ps_cm_snapshots\`（改坏可回滚）
- 方案：`%APPDATA%\PSMenu\profiles\`
- 日志：`%TEMP%\ps_cm_error.log`（托盘右键可直达）

## 命令行
```
PSMenu.exe            常驻启动（已驻留时无参双开=静默）
PSMenu.exe menu       呼出菜单（转发给已驻实例）
PSMenu.exe config     打开设置
PSMenu.exe "shot=menu;lk=text"   离线出图调试（不弹窗）
```

## 提示
- 脚本类菜单项需要 Photoshop 正在运行（COM 连接）。
- 菜单里直接打字可搜索：支持中文、拼音首字母（如 wzyx=文字阴影）、子序列模糊。
- 卸载：托盘退出 + 删掉本目录 + 关掉开机自启即可，全部数据在 %APPDATA%\PSMenu。
